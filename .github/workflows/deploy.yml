name: Staging

on:
  push:
    branches: ["development"]
  pull_request:
    branches: ["development"]

jobs:
  create-deployment-artifacts:
    name: Create deployment artifacts
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Compile CSS and Javascript
        run: |
          npm install
          npm run prod
      - name: Configure PHP 8.3
        uses: shivammathur/setup-php@master
        with: 
            php-version: 8.3
            extensions: mbstring, ctype, fileinfo, openssl, PDO, bcmath, json, tokenizer, xml
      - name: Composer install
        run: |
            composer install --no-dev --no-interaction --prefer-dist
      -name: Create staging artifact
          env:
            GITHUB_SHA: ${{ github.sha }}
          run: tar -czf "${GITHUB_SHA}".tar.gz --exclude=*.git --exclude=node_modules *
      - name: Store artifact for distribution
        uses: actions/upload-artifact@v3
        with: 
            name: app-build
            path: ${{github.sha}}.tar.gz

      - name: Export deployment matrix
        id: export-deployment-matrix
        run: |
            delimiter="$(openssl rand -hex 8)"
            JSON="$(cat ./.github/workflows/servers.json)"
            echo "DEPLOYMENT_MATRIX<<${delimiter}" >> "${GITHUB_OUTPUT}"
            echo "$JSON" >> "${GITHUB_OUTPUT}"
            echo "${delimiter}" >> "${GITHUB_OUTPUT}"
      # - name: Deploy to server
      #   run: |
      #     ssh -o StrictHostKeyChecking=no -i ${{secrets.SSH_KEY}} root@152.42.232.121 'ls'
      #     mkdir Home
      #     cd Home
      #     apt-get install git-all
      #     git clone git@github.com:https://github.com/Global-Supply-Chain/escexport-api.git
      #   # with:
      #   #   php-version: "8.2"
      #   #   host: ${{secrets.SSH_HOST}}
      #   #   key: ${{secrets.SSH_KEY}}
      #   #   username: ${{secrets.SSH_USERNAME}}

      #   #   script: |
      #   #     mkdir Home
      #   #     cd Home
      #   #     apt-get install git-all
      #   #     git clone git@github.com:https://github.com/Global-Supply-Chain/escexport-api.git
      # - uses: actions/checkout@v3
      # - name: Copy .env
      #   run: php -r "file_exists('.env') || copy('.env.example', '.env');"
      # - name: Install Dependencies
      #   run: composer install -q --no-ansi --no-interaction --no-scripts --no-progress --prefer-dist
      # - name: Generate key
      #   run: php artisan key:generate
      # - name: Directory Permissions
      #   run: chmod -R 777 storage bootstrap/cache
      # - name: Create Database
      #   run: |
      #     mkdir -p database
      #     touch database/database.mysql
      # - name: Execute tests (Unit and Feature tests) via PHPUnit/Pest
      #   env:
      #     DB_NAME: GSC_EXPORT
      #     DB_CONNECTION: mysql
      #     DB_DATABASE: database/database.mysql
      #   run: php artisan test
